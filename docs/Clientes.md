# Clientes

Estado: stub — completar cuando se trabaje acá.

**Archivos:** `src/features/clientes/components/ClientesView.tsx`, `NuevoClienteModal.tsx`

Ojo: sigue en el modelo de permisos viejo (`puede_operar_seccion`), no en el `usePuedeEditar` nuevo — ver
[[Permisos#Modelo Ver / Editar]]. `descuento_pct` por cliente ya funciona como mecanismo simple de
fidelización.

**Relacionado:** [[Permisos]], [[Ventas]], [[Index]]
